import { addContact, addContactWithUid, removeContact, updateContact, getContact, resetContact} from './actions';

export default {
    addContact,
    addContactWithUid,
    removeContact, 
    updateContact,
    getContact,
    resetContact
};
